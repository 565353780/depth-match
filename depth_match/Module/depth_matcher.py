import torch


class DepthMatcher(object):
    def __init__(self) -> None:
        return

    @staticmethod
    def matchDepth(
        source_depth: torch.Tensor,
        target_depth: torch.Tensor,
    ) -> torch.Tensor:
        return
